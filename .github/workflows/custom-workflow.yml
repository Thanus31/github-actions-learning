name: Custom Workflow
on:
  push:
    branches:
      - main
      - working-intermediate-thanusha
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Say Hello
        run: echo "Hello from custom workflow!"
